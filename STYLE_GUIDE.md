# mengr 代码风格指南

这份指南记录 `mengr` 0.0.3 采用的统一规则。目标不是追求
“最短代码”，而是让 Jin-Xin Meng 隔较长时间重新阅读时，仍能迅速理解输入、
处理步骤和输出。

## 1. 文件名与函数名

- R 文件名使用小写 kebab-case，并按功能加前缀：
  `utils-*`、`stats-*`、`profile-*`、`model-*`、`plot-*`。
- 函数名只使用小写字母、数字和下划线，例如
  `calcu_distance()`、`plot_taxa_boxplot()`。
- 不用连字符命名函数，因为 R 会把 `-` 解释为减号。
- 函数名中的缩写也使用小写形式，例如 `plot_pca()`、`plot_pcoa()`、
  `plot_nmds()`、`rc2tpm()`；帮助文本和普通字符串仍可使用标准大写缩写。
- 明显拼写错误直接修正，不保留旧拼写兼容函数。

## 2. 一般函数结构

较长函数按以下顺序组织：

1. 整理输入对象。
2. 检查列名、维度、数值范围和选项。
3. 按名称对齐 sample 或 feature。
4. 执行核心计算。
5. 整理输出列和顺序。
6. 返回结果，不隐式保存文件。

函数体使用简短的中文编号注释，例如 `## 2. 对齐样本`。注释应解释这一段
“为什么做”或“需要避免什么问题”，不写没有信息量的 `## 开始计算`。

## 3. 公开参数命名

- `profile`：feature × sample 的数值表。
- `data`：没有更具体语义时使用的通用数据框。
- `feature_meta`：feature 注释表，例如 taxonomy、pathway class 或其他 feature
  层面的描述信息。
- `sample_meta`：样本元数据表，包含样本标识、分组、批次或其他样本协变量。
- `sample_col`：样本标识列，默认 `"sample"`。
- `group_col`：分组列，默认 `"group"`。
- `group_level`：明确的分组顺序。
- `group_color`：与分组对应的颜色。
- `feature_col`：feature 标识列，通常默认 `"name"`。
- `library_size`：文库大小；表格形式在函数内部命名为 `library_size_df`，
  向量形式命名为 `library_size_vec`。

只要能够显式指定列名，就不假设 `feature_meta` 或 `sample_meta` 的第一列、第二列
必然具有某个含义。
不再使用 `group_rename`、`data_colnames` 这类一次重命名多个列的模糊接口。

## 4. 函数内部对象命名

- `profile_df`、`group_df`、`metadata_df`：整理后的数据框。
- `profile_mat`、`distance_mat`、`count_mat`：矩阵。
- `sample_vec`、`group_vec`、`feature_vec`：向量。
- `plot_df`：直接传给绘图层的数据。
- `test_df`：直接传给统计检验的数据。
- `result_df`：最终表格结果。
- `*_df`：数据框或 tibble；不使用 `plot_data`、`result_data` 这类含义相同但
  风格不一致的名称。
- `*_mat`：矩阵；`*_vec`：原子向量；`*_list`：列表。
- `*_obj`：模型或其他包生成的对象。
- `p`：最终 ggplot 对象。

避免 `dat`、`tmp`、`data1`、`result1` 等不能说明用途的名称。只有真正通用的输入
才叫 `data`；一旦进入具体阶段，就改为 `plot_df`、`test_df` 等角色名称。

## 5. 点前缀的使用边界

点前缀只用于包内共享、但不希望用户直接调用的对象：

- 内部辅助函数：`.check_columns()`、`.align_profile_group()`。
- 内部常量：`.default_group_colors`。

普通局部变量和只在一个公开函数中使用的局部 helper 不加点前缀。因此使用
`plot_df`、`label_vec`、`kld()`，而不是 `.plot_df`、`.label`、`.KLD()`。点前缀
不是“临时变量”的标记，而是 package-level internal API 的标记。

一个有限例外是管道内为避免覆盖用户原始字段而创建的短期辅助列，例如
`.input_order` 或 `.rank`。这类列必须只存在于函数内部，并在最终返回前删除
或改为对用户有明确含义的列名。

### Helper 拆分标准

不根据函数行数机械拆分 helper。满足以下至少一种实际场景时才拆分：

- 同一段逻辑被两个或更多函数调用。
- 同一规则在多处出现，必须集中维护以避免校验、转换或返回结果不一致。
- 该步骤能独立测试，并且未来很可能被其他分析流程复用。
- 该步骤本身是用户会在分析脚本中直接调用的完整操作。

只服务单个位置、依赖大量主函数局部状态，或拆出后需要传递很多参数的代码，
原则上留在主函数中。为了单纯缩短主函数而创建的一次性 helper 不保留。

- 供多个包内函数复用但不属于用户接口：使用 `.xxx()`，不添加 `@export`。
- 用户会直接调用的分析操作：使用普通函数名，添加完整 roxygen2 文档和
  `@export`。
- 私有 helper 只有在被多个功能模块共同使用时才移入 `internal-utils.R`；
  领域专用 helper 保留在对应的 `stats-*`、`plot-*` 或 `model-*` 文件顶部。

## 6. 参数选项与检查

- 有有限真实选项的参数使用 `match.arg()`。
- 默认值列出常用选项，例如
  `method = c("spearman", "pearson", "kendall")`。
- 如果参数同时允许自定义函数，先判断 `is.function()`，字符选项再用
  `match.arg()`。
- 检查错误信息应指出哪个对象缺少哪些列或哪个值超出范围。
- 样本顺序必须按名称显式对齐，不能依赖两个输入“碰巧同序”。

## 7. 依赖与语法

- 外部函数统一使用 `package::function()`。
- 所有管道统一使用 base R 的 `|>`，不导入 `%>%`。
- 使用 `TRUE` 和 `FALSE`，不使用 `T` 和 `F`。
- 生物学标识符相关数据框使用 `check.names = FALSE`。
- 矩阵和数据框筛选时保留 `drop = FALSE`。
- 使用 `seq_len()`、`seq_along()`，避免 `1:n` 在 `n = 0` 时产生错误。

## 8. 换行与排版

- 以 RStudio margin column 80 作为视觉参考。
- 80–100 字符的完整语句通常保留一行；超过约 100 字符或嵌套明显时换行。
- 函数定义换行后，参数按同一层级缩进。
- 简单函数调用不拆得过碎；复杂调用按参数语义分组。
- `+` 和 `|>` 放在上一行末尾，让阅读方向从上到下保持一致。

## 9. 注释与帮助文档

- 函数体内部 `##` 注释使用中文；不适合翻译的统计或编程关键词保留英文。
- roxygen2 帮助文档使用 UTF-8 双语：英文在前，中文随后。
- 帮助文档详细说明输入方向、参数、返回值、边界条件、依赖和可运行示例。
- 帮助文档回答“用户如何调用”，内部注释回答“代码为什么这样实现”。

## 10. 输出与副作用

- 绘图函数返回 plot object，不自动调用 `ggsave()`。
- 需要写文件的函数，函数名或帮助文档必须明确说明副作用。
- 计算函数尽量返回可继续处理的表格；复杂分析返回命名 list，并保留 model、
  tidy result 和 plot 所需数据。
- 不在 source 文件顶层打印消息、加载 package 或执行分析。
