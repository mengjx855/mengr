% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-heatmap.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Draw a heatmap with aligned row and column annotations}
\usage{
plot_heatmap(
  profile,
  scale = c("row", "none", "column"),
  border_color = NA,
  title = "Scaled value",
  cellwidth = 3,
  cellheight = 3,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  treeheight_row = 15,
  treeheight_col = 15,
  show_rownames = FALSE,
  show_colnames = FALSE,
  row_annotation = NULL,
  col_annotation = NULL,
  annotation_legend = TRUE,
  show_row_anno = NULL,
  show_col_anno = NULL,
  annotation_colors = NULL,
  ...
)
}
\arguments{
\item{profile}{A numeric matrix-like object with features in rows and samples
in columns.}

\item{scale}{Scaling direction: \code{"none"}, \code{"row"}, or \code{"column"}.}

\item{border_color}{Cell-border color. Use \code{NA} to hide borders.}

\item{title}{Heatmap legend title.}

\item{cellwidth, cellheight}{Cell dimensions passed to
\code{ComplexHeatmap::pheatmap()}.}

\item{cluster_rows, cluster_cols}{Whether to cluster rows or columns; an
\code{hclust} object is also accepted by the underlying function.}

\item{treeheight_row, treeheight_col}{Dendrogram sizes.}

\item{show_rownames, show_colnames}{Whether to show matrix names.}

\item{row_annotation, col_annotation}{Optional data frames whose \code{name} column
contains row or column names.}

\item{annotation_legend}{Whether to show annotation legends.}

\item{show_row_anno, show_col_anno}{Annotation columns to retain. \code{NULL} keeps
all columns other than \code{name}.}

\item{annotation_colors}{Optional named list of annotation color vectors.}

\item{...}{Additional arguments passed to \code{ComplexHeatmap::pheatmap()}.}
}
\value{
A ComplexHeatmap heatmap object, invisibly drawn according to the
behavior of \code{ComplexHeatmap::pheatmap()}.
}
\description{
Draw a \code{ComplexHeatmap::pheatmap()} heatmap from a numeric matrix. Annotation
rows are matched by the explicit \code{name} column, so their input order does not
need to match the profile. Missing categorical palettes are generated
automatically, while user-supplied palettes take precedence.
}
\details{
基于数值矩阵绘制 \code{ComplexHeatmap::pheatmap()} 热图。行、列注释表通过
\code{name} 列与 profile 对齐，因此输入顺序可以不同。分类变量缺失配色时会
自动生成，用户在 \code{annotation_colors} 中提供的配色优先。
}
