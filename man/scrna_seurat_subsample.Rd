% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-scrna.R
\name{scrna_seurat_subsample}
\alias{scrna_seurat_subsample}
\title{Subsample cells from a Seurat object}
\usage{
scrna_seurat_subsample(
  seurat,
  percent = 0.05,
  seed = 2026,
  stratified = FALSE,
  group_col = NULL,
  min_cells = 100,
  verbose = TRUE
)
}
\arguments{
\item{seurat}{A Seurat object.}

\item{percent}{Fraction of cells to retain, in the interval \verb{(0, 1]}.}

\item{seed}{Integer random seed.}

\item{stratified}{Whether to sample independently within groups.}

\item{group_col}{Metadata column used for stratification. \code{NULL} or
\code{"ident"} uses active Seurat identities.}

\item{min_cells}{Minimum cells retained per group during stratified sampling.}

\item{verbose}{Whether to report the original and retained cell counts.}
}
\value{
A Seurat object containing the selected cells. When \code{percent = 1},
the input object is returned unchanged.
}
\description{
Randomly retain a fraction of cells globally or independently within groups.
The returned cells are restored to their order in the input object.
}
\details{
With stratified sampling, groups smaller than \code{min_cells} are kept
in full and larger groups retain at least \code{min_cells} cells. Consequently,
cell-type proportions may differ from the input object.
}
