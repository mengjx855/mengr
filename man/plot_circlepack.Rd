% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-composition.R
\name{plot_circlepack}
\alias{plot_circlepack}
\title{Plot categorical composition with circle packing}
\usage{
plot_circlepack(
  data,
  name_col = "name",
  value_col = "n",
  name_level = NULL,
  decreasing = TRUE,
  top_n = NULL,
  other_name = "Other",
  other_last = TRUE,
  add_count = TRUE,
  add_percent = FALSE,
  title = NULL,
  fill = "auto",
  border_color = "white",
  border_size = 0.5,
  fill_alpha = 0.9,
  font_size = 3,
  font_color = "black",
  font_face = "plain",
  min_label_value = 1,
  show_legend = FALSE,
  root_name = "Total",
  percent_digits = 1,
  seed = 2026
)
}
\arguments{
\item{data}{A data frame containing category names and numeric values.}

\item{name_col}{Name of the category column in \code{data}.}

\item{value_col}{Name of the numeric-value column in \code{data}.}

\item{name_level}{Optional character vector defining category order. Listed
categories are placed first, followed by categories not present in the
vector. When supplied, this argument takes precedence over \code{decreasing}.}

\item{decreasing}{Whether categories are ordered by decreasing value when
\code{name_level} is \code{NULL}.}

\item{top_n}{Optional maximum number of displayed categories, including the
residual category. When more categories are present, the first
\code{top_n - 1} categories are retained and the remainder are combined.}

\item{other_name}{Label assigned to categories combined by \code{top_n}.}

\item{other_last}{Whether the residual category is placed last.}

\item{add_count}{Whether category values are included in circle labels.}

\item{add_percent}{Whether percentages are included in circle labels.}

\item{title}{Optional plot title.}

\item{fill}{Fill colors. Supply \code{"auto"} for the built-in palette, \code{"hue"}
for a hue palette, one color, an unnamed color vector, or a named vector
containing every displayed category.}

\item{border_color}{Circle-border color.}

\item{border_size}{Circle-border line width.}

\item{fill_alpha}{Circle-fill opacity.}

\item{font_size}{Label text size.}

\item{font_color}{Label text color.}

\item{font_face}{Label font face, such as \code{"plain"}, \code{"bold"}, or \code{"italic"}.}

\item{min_label_value}{Minimum category value required to display
a label. Categories below this threshold remain visible without labels.}

\item{show_legend}{Whether the category legend is displayed.}

\item{root_name}{Name assigned to the internal root node. It must not match
any displayed category name.}

\item{percent_digits}{Number of decimal places shown in percentage labels.}

\item{seed}{Random seed used to calculate the circle-packing layout. The
caller's random-number state is restored after layout calculation.}
}
\value{
A \code{ggraph} and \code{ggplot} object. Its \code{plot_df} attribute contains the
processed categories in columns \code{name}, \code{value}, \code{percent}, and \code{label}.
}
\description{
Draw a circle-packing composition plot in which each category is represented
by a circle with area proportional to its aggregated value.
}
\details{
Missing or empty category names, non-finite values, and non-positive values
are removed. Duplicate categories are combined by summing their values.

The function constructs a two-level graph containing one root node and one
leaf node per displayed category. Only leaf nodes are drawn. A fixed
coordinate ratio and symmetric plotting limits preserve circular geometry.

Category ordering may affect the spatial arrangement of circles but does
not change their values or relative areas. Circle positions do not represent
statistical significance, similarity or biological relationships.

Percentages use the sum of all valid input values as the denominator,
including values combined into the residual category.
}
\examples{
example_df <- data.frame(
  category = c("Bile acids", "Aromatic compounds", "Amines", "Organic acids"),
  n = c(7, 5, 2, 1)
)

if (
  requireNamespace("ggraph", quietly = TRUE) &&
    requireNamespace("tidygraph", quietly = TRUE) &&
    requireNamespace("withr", quietly = TRUE)
) {
  plot_circlepack(
    example_df,
    name_col = "category",
    value_col = "n",
    title = "Metabolite composition",
    add_percent = TRUE
  )
}

}
\seealso{
\code{\link[ggraph:ggraph]{ggraph::ggraph()}} and \code{\link[ggraph:create_layout]{ggraph::create_layout()}}
}
