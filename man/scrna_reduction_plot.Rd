% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-scrna.R
\name{scrna_reduction_plot}
\alias{scrna_reduction_plot}
\title{Plot a Seurat dimensional reduction with optional subsampling}
\usage{
scrna_reduction_plot(
  seurat,
  reduction = "umap",
  percent = 0.05,
  seed = 2026,
  stratified = FALSE,
  group_col = NULL,
  min_cells = 100,
  pt_size = 0.005,
  alpha = 0.7,
  label = TRUE,
  repel = TRUE,
  raster = FALSE,
  raster_dpi = 300,
  title = NULL,
  aspect_ratio = 1,
  legend_position = "right"
)
}
\arguments{
\item{seurat}{A Seurat object containing the requested reduction.}

\item{reduction}{Name of a reduction in \code{seurat@reductions}.}

\item{percent}{Fraction of cells to plot, in the interval \verb{(0, 1]}.}

\item{seed}{Integer seed used for sampling and repelled labels.}

\item{stratified}{Whether to sample independently within groups.}

\item{group_col}{Metadata column used for colors, labels, and stratified
sampling. \code{NULL} or \code{"ident"} uses active Seurat identities.}

\item{min_cells}{Minimum cells retained per group during stratified sampling.
Groups smaller than this value are retained in full.}

\item{pt_size, alpha}{Point size and opacity.}

\item{label}{Whether to add one label per group.}

\item{repel}{Whether labels should use \code{ggrepel::geom_text_repel()}.}

\item{raster}{Whether to rasterize points with
\code{ggrastr::geom_point_rast()}.}

\item{raster_dpi}{Resolution of the rasterized point layer.}

\item{title}{Optional plot title.}

\item{aspect_ratio}{Panel height-to-width ratio.}

\item{legend_position}{Legend position passed to \code{ggplot2::theme()}.}
}
\value{
A ggplot object. The complete plotting data and sampled plotting data
are stored in the \code{plot_df} and \code{sample_df} attributes, respectively.
}
\description{
Draw the first two dimensions of a reduction stored in a Seurat object.
Global or within-group subsampling can reduce plotting cost for large
single-cell and spatial-transcriptomics objects. Group-label coordinates are
always calculated from all cells, not only the plotted subset.
}
\details{
Stratified sampling can overrepresent rare groups because every
group retains at least \code{min_cells} cells when available. The displayed
proportions should therefore not be interpreted as population abundance.
}
