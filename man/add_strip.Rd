% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-ggplot2.R
\name{add_strip}
\alias{add_strip}
\title{Add alternating backgrounds to discrete ggplot2 panels}
\usage{
add_strip(
  p,
  fill = c("white", "grey95"),
  alpha = 1,
  axis = c("auto", "x", "y", "both")
)
}
\arguments{
\item{p}{A ggplot object.}

\item{fill}{Character vector of exactly two colors used alternately.}

\item{alpha}{Opacity of the background bands, between 0 and 1.}

\item{axis}{Axis to inspect. \code{"auto"} and \code{"both"} add bands to every
discrete axis; \code{"x"} or \code{"y"} restrict the operation to one axis.}
}
\value{
The input ggplot with background rectangle layers prepended.
}
\description{
Insert alternating rectangular background layers behind an existing ggplot.
Discrete x axes receive vertical bands and discrete y axes receive horizontal
bands. Because the colors are set outside \code{aes()}, an existing fill scale in
the plot is left unchanged.
}
\details{
Axis breaks are obtained from the first panel of the built plot.
Plots with free facet scales may therefore require adding backgrounds to
individual panels manually.
}
